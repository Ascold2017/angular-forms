import { ControlComponent } from './control.component';

describe('ControlComponent', () => {
  let component: ControlComponent;

  beforeEach(() => {
    component = new ControlComponent(null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
